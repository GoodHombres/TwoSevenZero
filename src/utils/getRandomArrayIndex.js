/**
 * getRandomArrayIndex - Return a random array index based on the count given
 *
 * @param {number} count
 */
export default function getRandomArrayIndex(count) {
  // Returns random index
  return Math.floor(Math.random() * count);
}
